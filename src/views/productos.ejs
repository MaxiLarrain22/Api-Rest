<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Muestra o Lista de Productos</title>
</head>
<body>
        <h1>Muestra o Lista de Productos</h1>
        <!-- <table>
            <thead>
                <tr>
                    <th>Titulo</th>
                    <th>Descripcion</th>
                </tr>
            </thead>
            <tbody>
                <% productos.forEach(productos => { %>
                <tr>
                    <td><%= producto.title %></td>
                    <td><%= producto.description %></td>
                </tr>
                <% }) %>
            </tbody>
        </table> -->
        <section class="products">
            <% productos.forEach(productos => { %>
            <h2><%= producto.title %></h2>
            <p><%= producto.description %></p>
            <% }) %>

        </section>
</body>
</html>